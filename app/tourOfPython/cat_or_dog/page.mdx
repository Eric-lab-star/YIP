export const metadata = {
  title: "CLI app · 고양이 또는 강아지를 추천해드려요",
  description:
    "지금까지 배운 input, 함수, if/elif/else 를 조립해서 반려동물을 추천하는 CLI 앱을 만들어봐요. (1부)",
};

<span id="title" />

# CLI app 고양이 또는 강아지를 추천해드려요

와~~~ 지금까지 파이썬에 대해서 정말 많은 것을 배웠어요. 😊 우선 가장 먼저 변수에 대해서 배웠고, 그 다음에 함수, `input()`, `type()`, `int()`, `if`, `else`, 마지막으로 `elif`까지 정말 많은 것을 배웠네요! 이만큼 배웠으면 이제 배운 것들을 열심히 조립해서 더 멋있는 프로그램을 만들 준비가 되었네요. 이번에 만들어볼 프로그램은 고양이나 강아지의 품종을 추천해 주는 프로그램을 만들어 볼 거예요.

웹에서 코드를 작성해야 한다면 [codesandbox.io 에서 코드를 작성하세요 🔗](https://codesandbox.io)

## 완성 예시

<video
  src="https://r2.kimkyungsub.com/cat_or_dog.mp4"
  preload="metadata"
  controls
  className="mx-auto h-70 bg-zinc-700"
/>

이렇게 문자로만 되어 있는 프로그램을 CLI app이라고 해요. CLI는 command line interface의 약자로 GUI와는 대비되는 앱이에요. 그래픽이 없는 제한된 환경에서 주로 사용되며, 개발자들이 편리하게 개발할 수 있게 도와주는 도구들이 많이 있어요. [더 많은 CLI 앱을 구경하고 싶으면 여기를 클릭 🔗](https://github.com/agarrharr/awesome-cli-apps)

이번에 만들 프로그램은 간단하지만 재미있는 프로그램이에요. 이 프로그램은 3가지 질문을 사용자에게 해서 고양이나 강아지를 추천할 거예요.

1. 고양이가 좋아 강아지가 좋아?
2. 검은색이 좋아 흰색이 좋아?
3. 원하는 크기 있어?

## 학습 목표

정확하지도 않으며 불편한 여러 가지 제약이 있는 프로그램을 왜 만들까요? 간단한 프로젝트를 만들면서 python 프로그래밍 언어에 익숙해지는 것이 목표에요. 구체적으로 이런 것들이 있어요.

1. `input()`을 사용해서 사용자의 입력을 처리하는 방법을 배운다.
2. `if..elif...else`의 사용 방법을 학습한다.
3. 함수를 사용해서 코드를 정리하는 방법을 익힌다.

<span id="start" />

## start() 함수 만들기

```python
def start():
    print("반려견 또는 반려묘를 추천해 드려요.")
```

함수를 배운 지 너무 오래되어서 잊어버린 건 아닌가요? 우리 프로그램의 모든 코드는 `start()` 함수가 실행될 때 실행하도록 만들 거예요. 이제 프로그램을 실행해서 출력창에 "반려견 또는 반려묘를 추천해 드려요."라는 문장이 나오는지 확인해보세요.

<span id="cat_or_dog" />

## cat or dog()

```python
def start():
    print("반려견 또는 반려묘를 추천해 드려요.")
    cat_dog = input("고양이가 좋아 강아지가 좋아?\n 고양이 또는 강아지 입력: ")  # <--- 새롭게 추가됨
```

이제 사용자가 원하는 게 고양이인지 강아지인지 확인을 해야겠네요. `input()`을 사용해서 쉽게 사용자가 무엇을 원하는지 확인할 수 있겠네요. 하지만 지금은 조금 문제가 생겼네요. 우리는 고양이나 강아지를 입력받기를 원하는데 만약 사용자가 코끼리를 입력해도 막을 방법이 없네요.

```python
def start():
    print("반려견 또는 반려묘를 추천해 드려요.")
    animal = cat_or_dog()

def cat_or_dog():
    cat_dog = input("고양이가 좋아 강아지가 좋아?\n 고양이 또는 강아지 입력: ")
    return cat_dog.strip()
```

이렇게 코드가 조금씩 늘어나고 새로운 기능을 추가해야 한다면 새로운 함수를 만들어서 분리하는 게 좋은 방법이에요. 이렇게 분리를 함으로써 start 함수를 더 간결하게 유지할 수 있고, cat_or_dog 함수만 수정하고 start 함수는 그대로 유지할 수 있게 되었어요.

<span id="raise_error" />

## 에러 만들기

```python
def cat_or_dog():
    cat_dog = input("고양이가 좋아 강아지가 좋아?\n 고양이 또는 강아지 입력: ").strip()  # <-- 추가
    if not (cat_dog == "고양이" or cat_dog == "강아지"):  # <-- 추가
        raise ValueError("오류!!!!!")  # <-- 추가
    return cat_dog
```

이번에는 오류를 잡아낼 수 있는 몇 가지 새로운 코드를 넣어봤어요. 가장 먼저, `.strip()`이라는 코드를 볼까요? 이 코드는 단어 앞뒤에 있을 수 있는 공백을 제거하는 기능을 해요. 공백 또한 코드로 인식을 하기 때문에 공백이 있는 단어와 없는 단어는 서로 같은 값이 아니에요.

[클릭해서 .strip() 더 알아보기 🔗](https://docs.python.org/3/library/stdtypes.html#str.strip)

그 다음으로는 입력한 값이 고양이도 아니고 강아지도 아닌 조건을 판단해주는 if 조건문을 추가했어요. cat_dog에 고양이라는 값이 있다면 조건문 `cat_dog == "고양이" or cat_dog == "강아지"`가 참이 돼요. 강아지일 때도 참이 되지요. 하지만 앞에 `not`을 추가해서 참인 값을 거짓으로 반전시켜버렸어요.

결국 고양이나 강아지가 아닌 값이라면 if의 조건문이 참이 되는 거예요. 이 상황은 프로그램이 원하는 상황이 아니므로 의도적으로 에러를 발생시켜서 프로그램을 중단시켜버릴 거예요. 그 코드가 다음에 나오는 `raise ValueError()`에요. `raise`는 의도적으로 오류를 발생시켜서 코드의 흐름을 바꾸는 방법이에요. `ValueError()`는 어떤 에러를 만들었는지 알려주는 코드에요. 에러를 만들고 회복시키지 않으면 프로그램이 종료됩니다. 이번에는 의도적으로 에러를 잡지 않아서 종료시킬 거예요. 이렇게 하면 고양이 또는 강아지만 값으로 얻을 수 있게 되었네요.

<span id="black_or_white" />

## black or white

그 다음으로는 어떤 색을 원하는지 확인할 거예요. 간단한 프로그램을 작성하기 위해서 흰색하고 검정색만 입력받을 수 있다고 제한을 했어요. 이번에도 앞에서 고양이와 강아지를 구별하고 잘못된 값은 걸러낸 방식을 그대로 사용할 수 있겠네요. 한번 스스로 만들어 볼까요?

```python
def black_or_white():
    color = input("검정색과 흰색중 하나를 입력하세요\n 검정색 또는 흰색 입력: ").strip()
    if not (color == "검정색" or color == "흰색"):
        raise ValueError("오류!!!!!")
    return color
```

<span id="small_medium_large" />

## small_medium_large()

이번에는 크기를 확인할 거예요. 이것도 같은 방식으로 함수를 만들어 주세요.

```python
def small_medium_large():
    size = input("원하는 크기가 있어?: \n 소형 또는 중형 또는 대형 입력: ").strip()
    if not (size == "소형" or size == "중형" or size == "대형"):
        raise ValueError("오류!!!!")
    return size
```

<span id="checkpoint" />

## 중간 점검

잘 따라오고 있나요? 지금까지 설명한 내용을 모두 작성했으면 아래와 같은 코드가 나왔을 거예요.

```python
def cat_or_dog():
    cat_dog = input("고양이가 좋아 강아지가 좋아?\n 고양이 또는 강아지 입력: ").strip()
    if not (cat_dog == "고양이" or cat_dog == "강아지"):
        raise ValueError("오류!!!!!")
    return cat_dog

def black_or_white():
    color = input("검정색과 흰색중 하나를 입력하세요\n 검정색 또는 흰색 입력: ").strip()
    if not (color == "검정색" or color == "흰색"):
        raise ValueError("오류!!!!!")
    return color

def small_medium_large():
    size = input("원하는 크기가 있어?: \n 소형 또는 중형 또는 대형 입력: ").strip()
    if not (size == "소형" or size == "중형" or size == "대형"):
        raise ValueError("오류!!!!")
    return size


def start():
    print("반려견 또는 반려묘를 추천해 드려요.")
    animal = cat_or_dog()
    color = black_or_white()
    size = small_medium_large()
```
